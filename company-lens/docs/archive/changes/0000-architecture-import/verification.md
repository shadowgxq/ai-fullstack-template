# 架构与前端复用交付核对

> 日期：2026-09-16｜本次任务：done｜运行验证：not_run

## 范围

导入用户提供的 repository-kit 产品、设计、开发规范和任务模板；增加当前 AI 架构总览、AI 开发流程与项目状态；复制指定既有前端。用户确认复用样式与交互、采用新接口规范。本任务完成不表示 D0、AI 服务或前后端联通完成。

## 实际核对

- 对照复制时 SHA-256 清单核对 395 个导入文件：22 个模板文件、373 个前端文件，无缺失。
- 前端业务源码、工程配置、测试及 pnpm 锁文件与来源一致；仅 AGENTS.md 增加本项目适用说明，已查看文本 diff。
- 模板差异集中于 README、AGENTS、导航、状态、命令、来源说明和 bootstrap 计划，适配前端已复制这一事实。
- 已核对新增主文档入口的相对链接；前端文件身份记录在 frontend-import-manifest.json。
- 排除的 .git、node_modules、dist、.env.local、.agents 和 .claude 未出现在目标前端。
- 原有两份 CompanyLens v1.0 文件未改动；v1.1 设计原文保留。
- 当前目录不能提供可用 Git 状态（git status 返回 not a git repository），因此使用来源文件差异与 SHA-256 比较，不声称已生成 Git 提交或仓库 diff。

## 未执行

未安装依赖、运行自动化测试、lint、typecheck、build、浏览器或服务；未执行数据库迁移、模型/搜索调用或部署。本次属于文档与原样复制，只核对文件差异，不新增行为测试。

## 已知边界

原前端仍含旧接口与 Railway 默认代理；其迁入不代表新接口联通。后续依据新 schema 替换前端 API/DTO/事件层，保留已完成的样式与交互。后端仍只有开发规则，无运行实现。
