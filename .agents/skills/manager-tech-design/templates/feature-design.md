# <复杂功能> 专项技术方案

上位总体方案：<路径与节>。适用 PRD 子句：<ID>。复杂性来源：<跨系统/状态/迁移/算法/安全>。

## 可观察目标与不变量

明确必须保持的行为、不允许出现的状态，以及范围之外的工作。

## 组件和契约

参与模块、所有者、输入输出、数据模型和依赖。不复制上位方案已有内容。

## 主流程与失败流程

用 sequence/state/flow Mermaid 描述真实路径；明确重试、超时、取消和恢复。

## 核心伪代码

用接近实现的伪代码表达主要分支、事务、幂等和验证；为关键边界写注释。
区分模型决定和确定性代码决定，注明如何校验模型输出（适用时）。

## 交付拆分候选

每个候选 change 都有独立验收结果；纯技术层列在 tasks，不为并行而拆虚假 change。

## 验证与发布

验收场景、回归、可观察性、故障恢复、迁移兼容和回滚。列出需人工批准的选择。
